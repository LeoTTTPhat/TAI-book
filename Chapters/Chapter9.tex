%%%%%%%%%%%%%%%%%%%%% Chapter 9 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Hardware-Rooted Trust: TEEs and Enclaves
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\motto{TEEs provide confidential computing: data is encrypted not just at rest and in transit, but also during processing.}

\chapter{Hardware-Rooted Trust: TEEs and Enclaves}
\label{chap:tees_enclaves}

\abstract{In this chapter, we explore how to anchor trust in the physical hardware layer through Trusted Execution Environments (TEEs). While cryptographic methods like Homomorphic Encryption offer mathematical protection, they often carry prohibitive performance costs. TEEs, such as Intel SGX \cite{costan2016} and ARM TrustZone, provide a high-performance alternative by isolating sensitive AI workloads in secure enclaves. We analyze the core properties of TEEs---Confidentiality, Integrity, Attestation, and Isolation---and demonstrate how they can be combined with statistical and cryptographic defenses to create a robust, defense-in-depth architecture.}

\section{When Cryptography Is Not Enough}
\label{sec:crypto_limitations}

As we saw in the previous chapters, cryptographic defenses like Secure Aggregation and Homomorphic Encryption are powerful, but they come with significant limitations. The performance overhead of HE can be 100--1000x slower than plaintext computation, and key management remains a major operational challenge. Furthermore, cryptographic protocols often assume an "honest-but-curious" adversary, offering less protection against a malicious operating system or hypervisor.

Trusted Execution Environments (TEEs) solve these problems by shifting the root of trust from software to hardware. By isolating computation in secure enclaves, we can protect data "in use," ensuring that even if the host machine's OS is compromised, the sensitive AI model and user data remain inaccessible.

\section{Core Properties of a TEE}
\label{sec:tee_properties}

A TEE provides four fundamental security guarantees that are vital for a Trustworthy AI Architect:

\begin{description}
  \item[Confidentiality:] 
        Data inside the enclave is encrypted at the hardware level; it is inaccessible to the OS, hypervisor, or co-located processes.
  \item[Integrity:]
        The code inside the enclave is measured before execution and cannot be tampered with, ensuring the AI model has not been modified.
  \item[Attestation:]
        A remote party can verify the authenticity and state of the enclave before sending sensitive data to it.
  \item[Isolation:]
        Enclave memory is physically isolated from the rest of the system's memory, preventing side-channel leakage.
\end{description}

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/ch9_tee.png}
\caption{Hardware-Rooted Trust: Architecture of a Trusted Execution Environment (TEE) showing the isolated Secure Enclave protecting data while in use.}
\label{fig:ch9_tee}
\end{figure}

\section{Major TEE Technologies: Hardware-Rooted Trust Comparison}
\label{sec:tee_technologies}

Four technologies dominate the confidential computing landscape in 2025--2026, each with distinct trust boundaries, memory limits, and operational models. The architect's choice determines whether to build \textbf{sovereign on-premise infrastructure} or leverage \textbf{cloud-native confidential computing}.

\begin{table}[ht]
\centering
\caption{Hardware-Rooted Trust Comparison: TEE technologies across the sovereignty--convenience spectrum. ASEAN relevance reflects deployment footprint in Vietnam, Indonesia, and Thailand.}
\label{tab:tee_comparison}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{p{2.4cm} p{1.5cm} p{2.2cm} p{2.2cm} p{3.3cm}}
\hline\noalign{\smallskip}
\textbf{Technology} & \textbf{Vendor} & \textbf{Enclave Memory} & \textbf{Attestation} & \textbf{Best For} \\
\noalign{\smallskip}\svhline\noalign{\smallskip}
Intel SGX (v2) & Intel & Up to 512\,GB EPC & DCAP remote attestation via Intel IAS & Server-side confidential model training and inference; on-premise sovereign clusters \\[3pt]
ARM TrustZone & ARM & Shared DRAM (partition) & OP-TEE + platform certificate & Mobile and IoT edge AI; on-device inference; \textbf{dominant in ASEAN smartphones} \\[3pt]
AMD SEV-SNP & AMD & Full VM memory & SNP attestation report (AMD root of trust) & VM-level isolation on AMD EPYC servers; public cloud Confidential VMs \\[3pt]
\rowcolor{patternbg}
AWS Nitro Enclaves & AWS & Up to 30\,GB & Nitro Attestation Document (signed by AWS root CA) & Managed confidential inference on AWS; lowest operational overhead; \textbf{not sovereign} \\
\noalign{\smallskip}\hline\noalign{\smallskip}
\end{tabular}
\end{table}

\begin{svgraybox}
\textbf{Sovereign vs.\ Cloud-Native Decision Framework:} If your system must satisfy Vietnam Decree 356's data sovereignty requirements, \textbf{Intel SGX or AMD SEV-SNP on on-premise hardware} is the only compliant choice — the attestation root of trust must not be a foreign cloud provider. AWS Nitro Enclaves are operationally excellent but attestation anchors to AWS's root CA, which sits outside Vietnamese legal jurisdiction. For pure model-serving use cases without personal data (e.g., inference on synthetic data), Nitro is a pragmatic shortcut. ARM TrustZone, embedded in every Qualcomm and MediaTek SoC in ASEAN, is the natural choice for on-device personalization where no data ever leaves the handset.
\end{svgraybox}

\section{Defense-in-Depth: The Composition of Trust}
\label{sec:tee_composition}

A truly resilient architecture does not rely on a single layer. The most secure systems utilize a composition of defenses:
\begin{equation}
\text{Total Protection} = \underbrace{\text{TEE}}_{\text{Hardware}} + \underbrace{\text{DP } (\epsilon, \delta)}_{\text{Statistical}} + \underbrace{\text{SecAgg}}_{\text{Cryptographic}}
\end{equation}

In this stack, the TEE protects the \emph{computation}, Secure Aggregation protects the \emph{transit}, and Differential Privacy protects the \emph{output} from being reverse-engineered.

\subsection{Unified Secure Distributed Blueprint}
\label{subsec:unified_blueprint}

Figure~\ref{fig:unified_blueprint} shows how these three defense layers nest together in a single data flow. An architect composing trust across a Federated Learning system should read this diagram left-to-right: data starts at the most sensitive point (inside the TEE on the client device) and moves outward through progressively coarser aggregation, with a privacy or security layer at each boundary.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[
  node distance=0.5cm and 0.0cm,
  font=\small,
  stage/.style={draw, rounded corners=4pt, inner sep=9pt,
                text width=3.2cm, align=center,
                minimum height=2.2cm},
  tee_s/.style={stage,   fill=red!8,    draw=red!60!black},
  dp_s/.style={stage,    fill=blue!8,   draw=blue!60!black},
  sec_s/.style={stage,   fill=orange!8, draw=orange!60!black},
  rob_s/.style={stage,   fill=green!8,  draw=green!60!black},
  glob_s/.style={stage,  fill=gray!8,   draw=gray!60},
  arrow/.style={->, >=Stealth, thick, gray!60},
  label_a/.style={font=\scriptsize\itshape, text=gray!70!black,
                  text width=1.6cm, align=center}
]

\node (tee) [tee_s]
    {\textbf{TEE Client}\\[4pt]
     Local training\\inside SGX enclave\\[4pt]
     {\scriptsize Gradient private\\from host OS}};

\node (dp) [dp_s, right=1.6cm of tee]
    {\textbf{DP-SGD}\\[4pt]
     Per-sample clip\\+ Gaussian noise\\$\mathcal{N}(0,\sigma^2 C^2\mathbf{I})$\\[4pt]
     {\scriptsize $\varepsilon$-budget consumed}};

\node (sec) [sec_s, right=1.6cm of dp]
    {\textbf{SecAgg}\\[4pt]
     Pairwise masking\\(Bonawitz protocol)\\[4pt]
     {\scriptsize Server sees only\\the masked sum}};

\node (rob) [rob_s, right=1.6cm of sec]
    {\textbf{Krum Filter}\\[4pt]
     Byzantine\\outlier rejection\\[4pt]
     {\scriptsize Malicious gradient\\discarded}};

\node (glob) [glob_s, right=1.6cm of rob]
    {\textbf{Global Model}\\[4pt]
     FedAvg update\\$\theta \leftarrow \theta - \eta\tilde{\mathbf{g}}$\\[4pt]
     {\scriptsize Clean, private,\\robust aggregate}};

\draw [arrow] (tee.east)  -- node[above, label_a] {raw\\gradient} (dp.west);
\draw [arrow] (dp.east)   -- node[above, label_a] {clipped +\\noised} (sec.west);
\draw [arrow] (sec.east)  -- node[above, label_a] {masked\\aggregate} (rob.west);
\draw [arrow] (rob.east)  -- node[above, label_a] {filtered\\update} (glob.west);

% Defense layer labels below the boxes
\node [below=0.5cm of tee,  font=\scriptsize\bfseries, red!70!black]  {Hardware layer};
\node [below=0.5cm of dp,   font=\scriptsize\bfseries, blue!70!black]  {Statistical layer};
\node [below=0.5cm of sec,  font=\scriptsize\bfseries, orange!70!black]{Cryptographic layer};
\node [below=0.5cm of rob,  font=\scriptsize\bfseries, green!70!black] {Integrity layer};
\node [below=0.5cm of glob, font=\scriptsize\bfseries, gray!70!black]  {Output};

\end{tikzpicture}
\caption{Unified Secure Distributed Blueprint. A gradient travels from the TEE-protected client (hardware confidentiality) through DP-SGD noise injection (statistical privacy), pairwise SecAgg masking (cryptographic transit protection), and Byzantine-Krum filtering (integrity) before updating the global model. Each boundary crossing is guarded by a distinct defense layer; no single layer's failure exposes the system end-to-end.}
\label{fig:unified_blueprint}
\end{figure}

\section{Practical Implementation: Secure Inference}
\label{sec:tee_implementation}

To implement secure inference inside an enclave (e.g., using the Open Enclave SDK), the architect must ensure that sensitive data only exists in plaintext within the enclave's "trusted" memory. Before leaving the enclave, all outputs must be re-encrypted, and the internal memory must be explicitly cleared to prevent any residual traces.

\section{Conclusion}
\label{sec:conclusion_tees}

By rooting trust in hardware, we enable AI to process sensitive data in environments we do not fully trust---such as public clouds or potentially compromised edge devices. However, hardware trust assumes a cooperative infrastructure. In the next chapter, we address the possibility of adversarial infrastructure through \textbf{Robust Aggregation}, ensuring the global model survives even when participants are actively malicious.


